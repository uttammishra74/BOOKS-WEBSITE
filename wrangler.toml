# Cloudflare Pages Configuration
# This file is used for Cloudflare Pages deployment
# https://developers.cloudflare.com/pages/

name = "freebookleaf"
compatibility_date = "2024-01-01"

# This is a static site configuration for Cloudflare Pages
# For static sites, most configuration is done in the Cloudflare Pages dashboard
# This file is minimal and mainly for Wrangler compatibility

[build]
command = "echo 'Static site - no build required'"
cwd = "."